<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=yes">

<!-- SEO Meta Tags -->
<meta name="description" content="VortexStore - Top up game termurah & tercepat di Indonesia 2026. Diamond ML, UC FF, Shell COD, Voucher Digital, Pulsa & Data all operator. Proses instan 24 jam, aman, terpercaya. Mulai dari Rp5.000!">
<meta name="keywords" content="top up game murah, top up ml, diamond mobile legends, top up free fire, uc ff, top up pubg, valorant points, voucher spotify netflix, pulsa murah, top up game indonesia">
<meta name="robots" content="index, follow">
<meta name="author" content="VortexStore Interactive">
<meta name="language" content="id">

<!-- Open Graph Tags (untuk social sharing) -->
<meta property="og:title" content="VortexStore | Top Up Game Termurah & Tercepat 24 Jam">
<meta property="og:description" content="Level up sekarang! Top up Diamond ML, UC FF, Genshin, Valorant, voucher Netflix/Spotify, pulsa & paket data instan. Harga paling murah, proses cepat, aman & terjamin.">
<meta property="og:image" content="https://yourdomain.com/assets/img/vortex-og.jpg"> <!-- Ganti dengan gambar OG ideal (1200x630 px) -->
<meta property="og:url" content="https://yourdomain.com/"> <!-- Ganti dengan URL asli situs -->
<meta property="og:type" content="website">
<meta property="og:site_name" content="VortexStore">
<meta property="og:locale" content="id_ID">

<!-- Twitter Card (opsional tapi bagus untuk X/Twitter) -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="VortexStore | Top Up Game Termurah & Tercepat">
<meta name="twitter:description" content="Top up game favoritmu dengan harga murah & proses instan 24 jam nonstop!">
<meta name="twitter:image" content="https://yourdomain.com/assets/img/vortex-og.jpg">

<!-- Tambahan modern (opsional tapi recommended) -->
<meta name="theme-color" content="#16181c"> <!-- Sesuai warna primary kamu -->
<meta name="msapplication-TileColor" content="#16181c">
<link rel="canonical" href="https://yourdomain.com/">


    <title>VortexStore | Premium Topup</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" integrity="sha384-tViUnnbYAV00FLIhhi3v/dWt3Jxw4gZQcNoSCxCIFNJVCx7/D55/wXsrNIRANwdD" crossorigin="anonymous">
    <style>
        :root {
            --primary: #00f2fe;
            --bg: #05070a;
            --glass: rgba(255, 255, 255, 0.08);
            --glass-border: rgba(255, 255, 255, 0.15);
            --nav-height: 65px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Poppins', sans-serif; }
        body { background-color: var(--bg); color: white; overflow-x: hidden; }

        nav {
            display: flex; justify-content: space-between; align-items: center;
            padding: 0 5%; background: rgba(5, 7, 10, 0.85); backdrop-filter: blur(15px);
            position: fixed; top: 0; left: 0; width: 100%; z-index: 2000;
            border-bottom: 1px solid var(--glass-border); height: var(--nav-height);
        }
        .logo { font-family: 'Orbitron', sans-serif; font-size: 1.15rem; }
        .logo span { color: var(--primary); }
        .btn-login { 
            border: 1px solid var(--primary); 
            padding: 5px 15px; 
            border-radius: 20px; 
            color: var(--primary); 
            font-size: 0.7rem; 
            font-weight: bold;
            text-decoration: none;
            display: inline-block;
        }

        .hero {
            height: 120vh;
            min-height: 400px;
            position: relative;
            overflow: hidden;
        }
        .hero-swiper {
            width: 100%;
            height: 100%;
        }
        .hero-slide {
            position: relative;
            width: 100%;
            height: 100%;
            background-size: cover;
            background-position: center;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
        }
        .hero-content {
            z-index: 10;
            padding: 20px;
            background: rgba(5,7,10,0.6);
            border-radius: 16px;
            max-width: 90%;
        }
        .hero h1 {
            font-family: 'Orbitron', sans-serif;
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-shadow: 0 0 20px var(--primary);
        }
        .hero p {
            font-size: 1rem;
            color: #ddd;
        }

        .hero-slide::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(to bottom, rgba(5,7,10,0.3), rgba(5,7,10,0.8));
            z-index: 1;
        }

        .category-badges {
            display: flex; gap: 8px; overflow-x: auto; padding: 12px 5%;
            scrollbar-width: none; background: #05070a;
            position: sticky; top: var(--nav-height); z-index: 1000;
            border-bottom: 1px solid var(--glass-border);
        }
        .category-badges::-webkit-scrollbar { display: none; }
        .cat-badge {
            flex: 0 0 auto; background: rgba(30,30,40,0.6); border: 1px solid var(--glass-border);
            border-radius: 999px; padding: 7px 18px; font-size: 0.75rem; color: #aaa; cursor: pointer;
            transition: all 0.2s;
        }
        .cat-badge.active { background: var(--primary); color: #000; font-weight: 700; border-color: var(--primary); }

        section { padding: 15px 5%; }
        .section-title { font-family: 'Orbitron', sans-serif; font-size: 0.85rem; border-left: 4px solid var(--primary); padding-left: 12px; margin-bottom: 20px; display: flex; align-items: center; gap: 8px; }

        .swiper { padding-bottom: 45px !important; }
        .swiper-pagination-bullet { background: #444 !important; opacity: 1; }
        .swiper-pagination-bullet-active { background: var(--primary) !important; width: 20px; border-radius: 5px; }

        .game-card {
            background: var(--glass); border-radius: 15px; overflow: hidden;
            border: 1px solid var(--glass-border); position: relative; aspect-ratio: 3 / 4.8;
        }
        .game-card img { 
            width: 100%; height: 100%; object-fit: cover; 
            opacity: 0;
            transition: opacity 0.5s ease;
        }
        .game-card img.loaded {
            opacity: 1;
        }
        .card-badge {
            position: absolute; top: 10px; left: 10px; z-index: 10;
            background: #ff3b3b; color: white; font-size: 0.6rem; padding: 3px 9px;
            border-radius: 5px; font-weight: 800; text-transform: uppercase;
            cursor: pointer; transition: all 0.2s;
        }
        .card-badge:hover { background: #d32f2f; transform: scale(1.08); }

        .card-info {
            position: absolute; bottom: 0; left: 0; right: 0; padding: 12px 8px;
            background: linear-gradient(transparent, rgba(0,0,0,0.9) 60%); text-align: center;
        }
        .card-info h3 { font-size: 0.75rem; margin-bottom: 8px; }
        .btn-pilih {
            width: 100%; padding: 7px; font-size: 0.7rem; border: none;
            border-radius: 8px; background: var(--primary); color: #000; font-weight: 800;
            cursor: pointer;
        }

        .voucher-card { 
            background: var(--glass); border-radius: 12px; border: 1px solid var(--glass-border); 
            aspect-ratio: 1/1; overflow: hidden; position: relative; 
        }
        .voucher-card img { 
            width: 100%; 
            height: 100%; 
            object-fit: cover; 
            opacity: 0;
            transition: opacity 0.5s ease;
        }
        .voucher-card img.loaded {
            opacity: 1;
        }
        .voucher-card img.pulsa-img {
            object-fit: contain; 
            padding: 12px; 
            background: #fff;
        }
        .voucher-title { 
            position: absolute; bottom: 0; width: 100%; background: rgba(0,0,0,0.85); 
            padding: 8px; font-size: 0.7rem; text-align: center; font-weight: 600;
            z-index: 5;
        }

        /* Skeleton styles */
        .skeleton {
            position: relative;
        }
        .skeleton::after {
            content: "";
            position: absolute;
            inset: 0;
            background: linear-gradient(110deg, #2a2a3a 30%, #3a3a4a 50%, #2a2a3a 70%);
            background-size: 200% 100%;
            animation: shimmer 1.5s infinite;
            z-index: 10;
            pointer-events: none;
            border-radius: 12px;
        }
        .skeleton .voucher-title {
            background: rgba(0,0,0,0.95);
            z-index: 15;
        }
        .skeleton .voucher-title .skeleton-text {
            height: 12px;
            background: #444;
            border-radius: 6px;
            margin: 0 auto;
            width: 60%;
            animation: pulse 1.5s infinite;
        }
        @keyframes shimmer {
            0% { background-position: -200% 0; }
            100% { background-position: 200% 0; }
        }
        @keyframes pulse {
            0%, 100% { opacity: 0.6; }
            50% { opacity: 1; }
        }

        .hidden-section { display: none !important; }
        .hidden-slide { display: none !important; }

        footer {
            background: rgba(10, 12, 18, 0.95);
            border-top: 1px solid var(--glass-border);
            padding: 40px 5% 20px;
            margin-top: 50px;
        }
       
        .footer-container {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: 40px;
            text-align: left;
        }
       
        .footer-info p {
            font-size: 0.85rem;
            color: #aaa;
            margin-top: -6%;
            line-height: 1.6;
        }
       
        .footer-links h4, .footer-contact h4 {
            font-family: 'Orbitron', sans-serif;
            font-size: 0.9rem;
            color: var(--primary);
            margin-bottom: 20px;
        }
       
        .footer-links ul { list-style: none; }
        .footer-links ul li { margin-bottom: 10px; }
        .footer-links ul li a {
            color: #ccc;
            text-decoration: none;
            font-size: 0.85rem;
            transition: 0.3s;
        }
        .footer-links ul li a:hover { color: var(--primary); padding-left: 5px; }
       
        .footer-contact p {
            font-size: 0.85rem;
            color: #ccc;
            margin-bottom: 10px;
        }
       
        .footer-bottom {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid rgba(255,255,255,0.05);
            font-size: 0.75rem;
            color: #555;
           
        }
       
        .footer-logo-img {
           height: 150px;
            width: 150px;
            display: block;
            margin-bottom: 15px;
            filter: drop-shadow(0 0 10px rgba(0, 242, 254, 0.3));
            margin-top: -5%;
            border-radius: 50%;
        }
       
        .blink-load {
            animation: blinker 1.5s linear 3;
        }
       
        @keyframes blinker {
            50% { opacity: 0; text-shadow: none; }
            100% { opacity: 1; text-shadow: 0 0 15px var(--primary); }
        }

        @media (max-width: 768px) {
            .footer-container { grid-template-columns: 1fr; gap: 30px; }
            .footer-info p { margin-top: -18%; }
            .hero-slide { background-position: center; height: 60vh; background-repeat: no-repeat; }
            .footer-logo-img { height: 110px; width:130px; }
.footer-info p {
            font-size: 0.85rem;
            color: #aaa;
            margin-top: 6%;
            line-height: 1.6;
        }
            .hero h1 { font-size: 1.8rem; }
            .hero {height: 60vh}
        }
      /* ============================================ */
      /* LIVE SEARCH STYLES - DENGAN GAMBAR */
      /* ============================================ */
      
      .search-container {
      position: relative;
      width: 540px;                    /* Lebar default desktop */
      max-width: 100%;
      margin: 0 10px;                  /* jarak dari logo & tombol login */
      }
      
      .search-input {
      width: 100%;
      height: 42px;                /* pastikan height fixed */
      padding: 0 50px 0 18px;      /* RUANG KHUSUS DI KANAN untuk tombol (minimal 45-55px) */
      border: 1px solid var(--glass-border);
      border-radius: 9999px;
      background: var(--glass);
      backdrop-filter: blur(12px);
      color: white;
      font-size: 0.92rem;
      outline: none;
      box-sizing: border-box;      /* pastikan padding tidak nambah lebar */
      }
      
      .search-btn {
      position: absolute;          /* WAJIB absolute */
      right: 6px;                  /* jarak dari tepi kanan input */
      top: 50%;                    /* vertikal center */
      transform: translateY(-50%) !important; /* centering vertikal sempurna */
      width: 34px;
      height: 34px;
      background: transparent;
      border: none;
      color: var(--primary);
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      z-index: 10;                 /* pastikan di atas input kalau ada overlap */
      }
      
      /* Optional: hover biar kelihatan interaktif */
      .search-btn:hover {
      background: rgba(0, 242, 254, 0.18);
      }
      
      /* Pastikan icon svg ukurannya pas */
      .search-btn svg {
      width: 20px;
      height: 20px;
      stroke: var(--primary);
      stroke-width: 2.5;
      }
      
      .search-results {
      position: absolute;
      top: calc(100% + 10px);
      left: 0;
      right: 0;
      background: rgba(15, 18, 26, 0.98);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(0, 242, 254, 0.2);
      border-radius: 20px;
      margin-top: 8px;
      max-height: 450px;
      overflow-y: auto;
      z-index: 2000;
      display: none;
      box-shadow: 0 30px 60px rgba(0, 0, 0, 0.8), 0 0 0 1px rgba(0, 242, 254, 0.1) inset;
      animation: slideDown 0.2s ease;
      }
      
      @keyframes slideDown {
      from {
      opacity: 0;
      transform: translateY(-10px);
      }
      to {
      opacity: 1;
      transform: translateY(0);
      }
      }
      
      .search-results-header {
      padding: 15px 20px 10px;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--primary);
      font-weight: 600;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      }
      
      .search-result-item {
      display: flex;
      align-items: center;
      padding: 12px 20px;
      color: white;
      text-decoration: none;
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      transition: all 0.3s ease;
      animation: fadeInUp 0.3s ease forwards;
      opacity: 0;
      transform: translateY(10px);
      align-items: center;
      }
      
      @keyframes fadeInUp {
      to {
      opacity: 1;
      transform: translateY(0);
      }
      }
      
      .search-result-item:hover {
      background: rgba(0, 242, 254, 0.1);
      padding-left: 25px;
      }
      
      .search-result-item:last-child {
      border-bottom: none;
      }
      
      .result-image {
      width: 50px;
      height: 50px;
      border-radius: 12px;
      overflow: hidden;
      margin-right: 15px;
      border: 2px solid rgba(0, 242, 254, 0.3);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
      flex-shrink: 0;
      }
      
      .result-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
      transition: transform 0.3s ease;
      }
      
      .search-result-item:hover .result-image img {
      transform: scale(1.1);
      }
      
      .result-info {
      flex: 1;
      min-width: 0;
      }
      
      .result-name {
      font-weight: 600;
      font-size: 0.95rem;
      margin-bottom: 4px;
      color: white;
      }
      
      .result-category {
      font-size: 0.7rem;
      color: var(--primary);
      opacity: 0.8;
      }
      
      .result-action {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--primary);
      font-size: 0.85rem;
      font-weight: 600;
      margin-left: 10px;
      opacity: 0;
      transform: translateX(-10px);
      transition: all 0.3s ease;
      }
      
      .search-result-item:hover .result-action {
      opacity: 1;
      transform: translateX(0);
      }
      
      .action-text {
      display: none;
      }
      
      @media (min-width: 768px) {
      .action-text {
      display: inline;
      }
      }
      
      .result-action svg {
      width: 18px;
      height: 18px;
      }
      
      /* No results styling */
      .no-results {
      padding: 40px 20px;
      text-align: center;
      color: #666;
      }
      
      .no-results svg {
      margin-bottom: 15px;
      color: var(--primary);
      opacity: 0.5;
      }
      
      .no-results p {
      font-size: 1rem;
      margin-bottom: 5px;
      color: white;
      }
      
      .no-results small {
      font-size: 0.8rem;
      color: #888;
      }
      
      .no-results.error svg {
      color: #ff3b3b;
      }
      
      /* Loading animation */
      .search-loading {
      padding: 30px 20px;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 15px;
      }
      
      .loading-spinner {
      width: 40px;
      height: 40px;
      border: 3px solid rgba(0, 242, 254, 0.1);
      border-top-color: var(--primary);
      border-radius: 50%;
      animation: spin 1s linear infinite;
      }
      
      .search-loading span {
      color: var(--primary);
      font-size: 0.9rem;
      }
      
      @keyframes spin {
      to { transform: rotate(360deg); }
      }
      
      /* Scrollbar styling */
      .search-results::-webkit-scrollbar {
      width: 6px;
      }
      
      .search-results::-webkit-scrollbar-track {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 10px;
      }
      
      .search-results::-webkit-scrollbar-thumb {
      background: var(--primary);
      border-radius: 10px;
      }
      
      .search-results::-webkit-scrollbar-thumb:hover {
      background: #00c4d4;
      }
      
      /* Responsive */
      @media (max-width: 768px) {
      .search-results {
      position: fixed;
      top: var(--nav-height);
      left: 5%;
      right: 5%;
      max-height: 70vh;
      border-radius: 16px;
      }
      .search-container {
    width: 240px;
      margin: 0 5px;
      flex: 1; /* Search bar akan melebar mengisi tengah */
      min-width: 0; /* Penting agar flex-item bisa mengecil */
      }
     /* Input pencarian */
     .search-input {
     display: block !important;
     width: 100%;
     height: 32px;
     background: rgba(255, 255, 255, 0.1) !important; /* Memaksa background muncul */
     border: 1px solid var(--glass-border) !important;
     padding: 6px 30px 6px 10px !important; /* Padding kanan untuk space ikon */
     margin-top: 5%;
     font-size: 0.7rem;
     border-radius: 15px;
     color: white;
     }
     
     .search-container {
     width: 130px; /* Batasi lebar agar tidak tabrakan dengan logo/login */
     margin: 0 5px;
     position: relative;
     }
     .search-btn {
     right: 8px !important;
     top: 50% !important; /* Menempatkan ikon di tengah vertikal */

     transform: translateY(-50%) !important;
     background: transparent !important;   /* pastikan tidak ada background */
     border: none !important;
     position: absolute;
     }
     
     .search-btn svg {
     width: 100%; /* Kecilkan ikon sedikit untuk mobile */
     height: 18px;
     
     
     }
     
     .logo {
     font-size: 17px; /* Perkecil logo agar muat */
     }
     
     .btn-login {
     padding: 4px 2px;
     
     height: 25px;
     font-size: 0.6rem;
     white-space: nowrap;
     }
     /* 4. Sembunyikan teks "Hi, User" di mobile jika layar terlalu sempit */
      }
      
      .user-dropdown {
      position: relative;
      display: inline-block;
      }
      
      .user-btn {
      background: none;
      border: none;
      cursor: pointer;
      display: flex;
      align-items: center;
      padding: 4px 8px;
      border-radius: 20px;
      transition: background 0.2s;
      }
      
      .user-btn:hover {
      background: rgba(0, 242, 254, 0.08);
      }
      
      .dropdown-content {
      display: none;
      position: absolute;
      right: 0;
      top: 100%;
      background: rgba(15,18,26,0.98);
      backdrop-filter: blur(12px);
      border: 1px solid var(--glass-border);
      border-radius: 12px;
      min-width: 180px;
      box-shadow: 0 15px 35px rgba(0,0,0,0.6);
      z-index: 1500;
      margin-top: 8px;
      overflow: hidden;
      }
      
      .dropdown-content a {
      display: block;
      padding: 10px 16px;
      color: white;
      text-decoration: none;
      font-size: 0.9rem;
      transition: all 0.2s;
      }
      
      .dropdown-content a:hover {
      background: rgba(0, 242, 254, 0.12);
      padding-left: 20px;
      }
      
      .dropdown-content.show {
      display: block;
      animation: fadeIn 0.2s ease;
      }
      
      /* Rotasi icon saat dropdown aktif */
      .dropdown-icon.active {
      transform: rotate(180deg);
      }
      
      @media (max-width: 768px) {
      .username-display {
      display: none;  /* Sembunyikan nama di mobile biar compact */
      }
      .dropdown-content {
      right: auto;
      left: 50%;
      transform: translateX(-50%);
      min-width: 160px;
      }
      
      
      /* Promo count badge base styling */
      .promo-count-badge {
      display: inline-block;
      min-width: 20px;
      height: 20px;
      line-height: 20px;
      padding: 0 6px;
      border-radius: 10px;
      font-size: 0.65rem;
      font-weight: bold;
      margin-left: 5px;
      text-align: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.3);
      animation: countPulse 2s infinite;
      background: linear-gradient(135deg, #ff3b3b, #ff6b6b);
      color: white;
      }
      
      /* Variasi warna per kategori */
      .promo-count-badge.hot-badge {
      background: linear-gradient(135deg, #ff6b6b, #ff8e8e);
      }
      
      .promo-count-badge.new-badge {
      background: linear-gradient(135deg, #ffd700, #ffed4e);
      color: #333;
      }
      
      .promo-count-badge.sale-badge {
      background: linear-gradient(135deg, #4caf50, #6fbf73);
      }
      
      .promo-count-badge.pc-badge {
      background: linear-gradient(135deg, #9c27b0, #ba68c8);
      }
      
      @keyframes countPulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.1); }
      }
      
      /* Animasi bell untuk promo */
      #promo-badge i {
      animation: bellRing 3s infinite;
      transform-origin: top;
      }
      
      @keyframes bellRing {
      0%, 100% { transform: rotate(0deg); }
      5% { transform: rotate(15deg); }
      10% { transform: rotate(-15deg); }
      15% { transform: rotate(10deg); }
      20% { transform: rotate(-10deg); }
      25% { transform: rotate(5deg); }
      30% { transform: rotate(0deg); }
      }
      
      /* Ikon colors */
      .cat-badge i {
      margin-right: 5px;
      transition: all 0.3s ease;
      }
      
      .cat-badge[data-filter="PROMO"] i { color: #ff3b3b; }
      .cat-badge[data-filter="HOT"] i { color: #ff6b6b; }
      .cat-badge[data-filter="NEW"] i { color: #ffd700; }
      .cat-badge[data-filter="SALE"] i { color: #4caf50; }
      .cat-badge[data-filter="PC"] i { color: #9c27b0; }
      .cat-badge[data-filter="PULSA"] i { color: #00f2fe; }
      }

      .modal-overlay {
    display: none; /* Sembunyi secara default */
    position: fixed;
    top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(0, 0, 0, 0.85);
    backdrop-filter: blur(8px);
    z-index: 9999;
    justify-content: center; align-items: center;
}

.modal-content {
    background: #0b0e14;
    border: 1px solid rgba(0, 242, 254, 0.3);
    border-radius: 20px;
    padding: 20px;
    max-width: 500px;
    position: relative;
    text-align: center;
    box-shadow: 0 0 30px rgba(0, 242, 254, 0.2);
}

.close-btn {
    position: absolute;
    top: 15px; right: 20px;
    color: white; font-size: 28px; cursor: pointer;
}

.modal-content img { width: 100%; border-radius: 10px; margin-bottom: 15px; }
.btn-action {
    background: linear-gradient(45deg, #00f2fe, #4facfe);
    border: none; padding: 12px 25px; border-radius: 8px;
    font-weight: bold; cursor: pointer; margin-top: 15px;
}
      /* Mengubah warna spinner SweetAlert2 */
      .swal2-loader {
      border-color: #00f2fe transparent #00f2fe transparent !important;
      }
      
      /* Opsional: Membuat popup loading lebih ramping/bersih */
      .swal2-popup.swal2-modal.swal2-loading {
      padding: 2em;
      border: 1px solid rgba(0, 242, 254, 0.3);
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0, 242, 254, 0.1);
      }
      
      /* Menghilangkan border biru default pada spinner di beberapa browser */
      .swal2-styled.swal2-confirm:focus {
      box-shadow: none !important;
      }
      
      .jumbotron-simple {
      background: linear-gradient(135deg, #0a0e17 0%, #05070a 100%);
      padding: 90px 5% 70px;
      text-align: center;
      border-bottom: 1px solid rgba(0, 242, 254, 0.12);
      }
      
      .jumbotron-simple h1 {
      font-family: 'Orbitron', sans-serif;
      font-size: 3rem;
      margin-bottom: 16px;
      text-shadow: 0 0 25px rgba(0, 242, 254, 0.35);
      }
      
      .jumbotron-simple h1 span {
      color: var(--primary);
      }
      
      .jumbotron-simple p {
      font-size: 1.2rem;
      color: #b0c4de;
      margin-bottom: 30px;
      max-width: 680px;
      margin-left: auto;
      margin-right: auto;
      }
      
      .btn-jumbo {
      padding: 14px 40px;
      background: var(--primary);
      color: #000;
      font-weight: 700;
      font-size: 1.1rem;
      border-radius: 50px;
      text-decoration: none;
      transition: all 0.3s;
      box-shadow: 0 6px 20px rgba(0, 242, 254, 0.3);
      }
      
      .btn-jumbo:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 30px rgba(0, 242, 254, 0.45);
      background: #00d4e6;
      }
      
      @media (max-width: 768px) {
      .jumbotron-simple { padding: 70px 5% 50px; }
      .jumbotron-simple h1 { font-size: 2.3rem; }
      .jumbotron-simple p  { font-size: 1.05rem; }
      }
      
      
      .help-jumbotron {
      background: rgba(15, 18, 26, 0.7);
      backdrop-filter: blur(10px);
      border: 1px solid var(--glass-border);
      border-radius: 12px;
      padding: 12px !important;
      margin-top: 10px;
      }
      
      .help-jumbotron p {
      font-size: 0.75rem !important;
      color: #aaa;
      margin-bottom: 8px;
      text-align: left;
      }
      
      .dropdown-contact {
      position: relative;
      width: 50%;
      }
      
      .btn-contact-toggle {
      width: 100%;
      padding: 8px 12px;
      background: var(--primary);
      color: #000;
      font-weight: 600;
      font-size: 0.8rem;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: all 0.2s;
      }
      
      .btn-contact-toggle:hover {
      background: #00d4e6;
      }
      
      .dropdown-contact-content {
      display: none;
      position: absolute;
      bottom: 100%;
      left: 0;
      right: 0;
      background: rgba(15, 18, 26, 0.98);
      backdrop-filter: blur(12px);
      border: 1px solid var(--glass-border);
      border-radius: 8px;
      margin-bottom: 5px;
      overflow: hidden;
      box-shadow: 0 -5px 20px rgba(0,0,0,0.5);
      z-index: 1000;
      font-size: 0.75rem;
      }
      
      .dropdown-contact-content.show {
      display: block;
      animation: fadeInUp 0.2s ease;
      }
      
      .contact-item {
      display: flex;
      align-items: center;
      padding: 8px 12px;
      color: white;
      text-decoration: none;
      border-bottom: 1px solid rgba(255,255,255,0.05);
      transition: all 0.2s;
      font-size: 0.75rem;
      }
      
      .contact-item:last-child {
      border-bottom: none;
      }
      
      .contact-item:hover {
      background: rgba(0, 242, 254, 0.1);
      padding-left: 15px;
      }
      
      .contact-item i {
      margin-right: 8px;
      color: var(--primary);
      }
      
      .contact-note {
      padding: 6px 8px;
      background: rgba(0,0,0,0.3);
      color: #888;
      text-align: center;
      border-top: 1px solid rgba(255,255,255,0.05);
      }
      
      /* Animasi */
      @keyframes fadeInUp {
      from { 
      opacity: 0; 
      transform: translateY(5px); 
      }
      to { 
      opacity: 1; 
      transform: translateY(0); 
      }
      }
      
      /* Rotasi icon */
      .dropdown-icon-contact.active {
      transform: rotate(180deg);
      }
      
      /* Responsive mobile */
      @media (max-width: 768px) {
      .help-jumbotron {
      padding: 10px !important;
      }
      
      .btn-contact-toggle {
      padding: 6px 10px;
      font-size: 0.75rem;
      }
      
      .contact-item {
      padding: 6px 10px;
      font-size: 0.7rem;
      }
      
      .contact-item i {
      font-size: 1rem !important;
      width: 20px !important;
      }
      
      .dropdown-contact-content {
      font-size: 0.7rem;
      }
      }
    </style>
</head>
<body>
    <nav>
        <div class="logo blink-load">VORTEX<span>STORE</span></div>
<!-- Search bar baru -->
    <div class="search-container">
        <input type="text" id="searchInput" placeholder="Cari game" class="search-input">
        <button class="search-btn">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M21 21L16.65 16.65M19 11C19 15.4183 15.4183 19 11 19C6.58172 19 3 15.4183 3 11C3 6.58172 6.58172 3 11 3C15.4183 3 19 6.58172 19 11Z" stroke="var(--primary)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </button>
    </div>
     <?php if ($this->session->userdata('logged_in')): ?>
     <div class="user-dropdown">
     <button class="user-btn" id="userBtn">
     <?php 
     $avatar = $this->session->userdata('avatar');
     if ($avatar): ?>
     <img src="<?= $avatar ?>" alt="Profile" 
     style="width: 34px; height: 34px; border-radius: 50%; border: 2px solid #212429; object-fit: cover;">
     <?php else: ?>
     <div style="width: 34px; height: 34px; border-radius: 50%; background: #6f7275; color: #000; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 1rem;">
     <?= strtoupper(substr($this->session->userdata('username'), 0, 1)) ?>
     </div>
     <?php endif; ?>
     
     <span class="username-display" style="font-size: 0.85rem; color: white; margin: 0 6px;"><?= $this->session->userdata('username') ?></span>
     
     <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="var(--primary)" stroke-width="2" class="dropdown-icon">
     <polyline points="6 9 12 15 18 9"></polyline>
     </svg>
     </button>
     
     <div class="dropdown-content" id="dropdownContent">
     <a href="<?= base_url('user/profile') ?>">Profil Saya</a>
     <a href="<?= base_url('dashboard') ?>">Dashboard</a>
     <a href="<?= base_url('dashboard/riwayat') ?>">Riwayat Pesanan</a>
     
     
     <hr style="border: none; border-top: 1px solid var(--glass-border); margin: 6px 0;">
     <a href="<?= base_url('auth/logout') ?>" style="color: #ff3b3b;">Logout</a>
     </div>
     </div>
     <?php else: ?>
     <a href="<?= base_url('auth') ?>" class="btn-login" style="border-color: var(--primary); color: var(--primary);">LOGIN / REGISTER</a>
     <?php endif; ?>
     <!-- BADGE PROMO DENGAN ANGKA -->
    
     </div>
    </nav>

    <div class="hero">
        <div class="swiper hero-swiper">
            <div class="swiper-wrapper">
                <div class="swiper-slide hero-slide" style="background-image: url('assets/img/banner/vortexstore.png');">
                    <div class="hero-content">
                        <h1>LEVEL UP NOW</h1>
                        <p>Top Up Game Tercepat & Termurah 24 Jam</p>
                    </div>
                </div>
                <div class="swiper-slide hero-slide" style="background-image: url('assets/img/banner/vortexstore-wallpaper.png');">
                    <div class="hero-content">
                        <h1>VORTEX POWER</h1>
                        <p>Aman, Cepat, Terpercaya</p>
                    </div>
                </div>
            </div>
            <div class="swiper-pagination hero-pagination"></div>
            <div class="swiper-button-next"></div>
            <div class="swiper-button-prev"></div>
        </div>
    </div>
    
    <div class="jumbotron-simple">
    <div class="container">
    <h1>VortexStore – <span>24 Jam Nonstop</span></h1>
    <p>Top up game & voucher digital • Proses cepat • Pembayaran aman semua metode</p>
    <a href="#sec-populer" class="btn-jumbo">Top Up Instan</a>
    </div>
    </div>
    
    

    <div class="category-badges">
    <span class="cat-badge active" data-filter="all">
    <i class="fas fa-th-large"></i> Semua
    </span>
    
    <span class="cat-badge" data-filter="PROMO" id="promo-badge">
    <i class="fas fa-bell"></i> 
    PROMO
    <?php if (isset($promo_count) && $promo_count > 0): ?>
    <span class="promo-count-badge"><?= $promo_count ?></span>
    <?php endif; ?>
    </span>
    
    <span class="cat-badge" data-filter="HOT">
    <i class="fas fa-fire"></i> 
    HOT
    <?php if (isset($hot_count) && $hot_count > 0): ?>
    <span class="promo-count-badge hot-badge"><?= $hot_count ?></span>
    <?php endif; ?>
    </span>
    
    <span class="cat-badge" data-filter="NEW">
    <i class="fas fa-star"></i> 
    NEW
    <?php if (isset($new_count) && $new_count > 0): ?>
    <span class="promo-count-badge new-badge"><?= $new_count ?></span>
    <?php endif; ?>
    </span>
    
    <span class="cat-badge" data-filter="SALE">
    <i class="fas fa-tag"></i> 
    SALE
    <?php if (isset($sale_count) && $sale_count > 0): ?>
    <span class="promo-count-badge sale-badge"><?= $sale_count ?></span>
    <?php endif; ?>
    </span>
    
    <span class="cat-badge" data-filter="PC">
    <i class="fas fa-desktop"></i> 
    PC
    <?php if (isset($pc_count) && $pc_count > 0): ?>
    <span class="promo-count-badge pc-badge"><?= $pc_count ?></span>
    <?php endif; ?>
    </span>
    
    <span class="cat-badge" data-filter="PULSA">
    <i class="fas fa-phone-alt"></i> PULSA
    </span>
    </div>

    <section id="sec-populer">
        <div class="section-title">🔥 GAME POPULER</div>
        <div class="swiper swiperPopuler">
            <div class="swiper-wrapper" id="list-populer"></div>
            <div class="swiper-pagination"></div>
        </div>
    </section>

    <section id="sec-terbaru">
        <div class="section-title">✨ GAME TERBARU</div>
        <div class="swiper swiperTerbaru">
            <div class="swiper-wrapper" id="list-terbaru"></div>
            <div class="swiper-pagination"></div>
        </div>
    </section>
    
    
    

    <section id="sec-pc">
        <div class="section-title">🖥️ GAME PC POPULER</div>
        <div class="swiper swiperPC">
            <div class="swiper-wrapper" id="list-pc"></div>
            <div class="swiper-pagination"></div>
        </div>
    </section>
    
    <section id="sec-voucher" class="hidden-section">
    <div class="section-title">🎟️ VOUCHER DIGITAL</div>
    <div class="swiper swiperVoucher">
    <div class="swiper-wrapper" id="list-voucher"></div>
    <div class="swiper-pagination"></div>
    </div>
    </section>
    
    <section id="sec-pulsa">
    <div class="section-title">📱 PULSA & PAKET DATA</div>
    <div class="swiper swiperPulsa">
    <div class="swiper-wrapper" id="list-pulsa"></div>
    <div class="swiper-pagination"></div>
    </div>
    </section>
   
    <footer>
        <div class="footer-container">
            <div class="footer-info">
                <img src="assets/img/banner/a.png" alt="VortexStore Logo" class="footer-logo-img">
                <p>VortexStore adalah platform top up game dan voucher digital termurah, tercepat, dan terpercaya di Indonesia. Nikmati layanan 24 jam nonstop dengan berbagai pilihan metode pembayaran instan.</p>
            </div>
            <div class="footer-links">
                <h4>Navigasi</h4>
                <ul>
                    <li><a href="#">Halaman Utama</a></li>
                    <li><a href="#">Daftar Harga</a></li>
                    <li><a href="#">Lacak Pesanan</a></li>
                    <li><a href="#">Syarat & Ketentuan</a></li>
                </ul>
            </div>
            <div class="footer-contact">
            <h4>Bantuan & Dukungan</h4>
            
            <!-- Tombol utama style compact -->
            <div class="help-jumbotron" style="padding: 15px;">
            
            <!-- Dropdown trigger -->
            <div class="dropdown-contact">
            <button class="btn-contact-toggle" id="contactToggle">
            Hubungi Kami
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-headset" viewBox="0 0 16 16">
            <path d="M8 1a5 5 0 0 0-5 5v1h1a1 1 0 0 1 1 1v3a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V6a6 6 0 1 1 12 0v6a2.5 2.5 0 0 1-2.5 2.5H9.366a1 1 0 0 1-.866.5h-1a1 1 0 1 1 0-2h1a1 1 0 0 1 .866.5H11.5A1.5 1.5 0 0 0 13 12h-1a1 1 0 0 1-1-1V8a1 1 0 0 1 1-1h1V6a5 5 0 0 0-5-5"/>
            </svg>
            </button>
            
            <div class="dropdown-contact-content" id="contactDropdown">
            <!-- WhatsApp -->
            <a href="https://wa.me/6281234567890?text=Halo%20VortexStore,%20saya%20ada%20pertanyaan%20tentang%20top%20up..." target="_blank" class="contact-item">
            <i class="fab fa-whatsapp" style="font-size: 1.2rem; width: 25px;"></i>
            <span>WhatsApp</span>
            
            </a>
            
            <!-- Email -->
            <a href="mailto:support@vortexstore.id?subject=Pertanyaan%20Top%20Up" class="contact-item">
            <i class="fas fa-envelope" style="font-size: 1.2rem; width: 25px;"></i>
            <span>Email</span>
            
            </a>
            
            <!-- Catatan kecil -->
            <div class="contact-note" style="padding: 8px; font-size: 0.7rem;">
           
            </div>
            </div>
            </div>
            </div>
            </div>
            
            
            
        <div class="footer-bottom">
            © 2026 VortexStore Interactive. All Rights Reserved.
        </div>
    </footer>
<div id="vortexModal" class="modal-overlay">
    <div class="modal-content">
        <span class="close-btn" onclick="closeModal()">&times;</span>
        <img src="assets/img/banner/a.png" alt="Promo">
        <div class="modal-text">
            <h2>NEW EVENT!</h2>
            <p>Dapatkan cashback hingga 50.000 untuk pengguna baru WhatsApp Login.</p>
            <button class="btn-action">CEK SEKARANG</button>
        </div>
    </div>
</div>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
    // ============================================
    // DATA GAME (Untuk frontend display)
    // ============================================
    const dbPopuler = [
    { n: "Mobile Legends", i: "assets/img/ml.jpg", b: "HOT" },
    { n: "Clash Of Clans", i: "assets/img/coc.png", b: "HOT" },
    { n: "Honor of Kings", i: "assets/img/hok.png", b: "PROMO" },
    { n: "Valorant", i: "assets/img/valo.jpg", b: "PROMO" },
    { n: "Free Fire", i: "assets/img/freefire.png", b: "SALE" },
    { n: "PUBG Mobile", i: "assets/img/pubg.jpg", b: "HOT" },
    { n: "Arena Of Valor", i: "assets/img/aov.png", b: "HOT" }
    ];
    
    const dbTerbaru = [
    { n: "Apex Legends", i: "assets/img/apex.jpeg", b: "PROMO" },
    { n: "Genshin Impact", i: "assets/img/genshin.jpeg", b: "NEW" },
    { n: "Call of Duty", i: "assets/img/callof.png", b: "PROMO" },
    { n: "Roblox", i: "assets/img/roblox.jpg", b: "HOT" },
    { n: "Delta Force", i: "assets/img/delta.png", b: "HOT" }
    ];
    
    const dbPC = [
    { n: "Point Blank", i: "assets/img/pb.png", b: "PC" },
    { n: "Valorant", i: "assets/img/valo.jpg", b: "PC" },
    { n: "Genshin Impact PC", i: "assets/img/genshin.jpeg", b: "PC" },
    { n: "Counter-Strike 2", i: "assets/img/cs.jpg", b: "HOT" },
    { n: "Dota 2", i: "assets/img/dota.jpg", b: "PC" },
    { n: "Lost Ark", i: "assets/img/lostark.jpg", b: "NEW" }
    ];
    
    const dbVoucher = [
    { n: "Spotify", i: "assets/img/spotify.jpg" },
    { n: "Netflix", i: "assets/img/netflix.jpg" },
    { n: "PlayStation", i: "assets/img/playstation.jpg" },
    { n: "Google Play", i: "assets/img/googleplay.png" }
    ];
    
    const dbPulsa = [
    { n: "Telkomsel", i: "assets/img/telkomsel.png" },
    { n: "Indosat", i: "assets/img/indosat.png" },
    { n: "XL Axiata", i: "assets/img/xl.png" },
    { n: "Tri (3)", i: "assets/img/tri.png" },
    { n: "Smartfren", i: "assets/img/smartfren.png" }
    ];
    
    // ============================================
    // FUNGSI PRELOAD IMAGE
    // ============================================
    function preloadImage(src) {
    return new Promise((resolve, reject) => {
    const img = new Image();
    img.onload = () => resolve(src);
    img.onerror = () => reject(src);
    img.src = src;
    });
    }
    
    // ============================================
    // RENDER SKELETONS
    // ============================================
    function renderSkeletons() {
    const count = 5;
    const skeletonHTML = Array(count).fill(`
    <div class="swiper-slide">
    <div class="voucher-card skeleton">
    <div class="voucher-title">
    <div class="skeleton-text"></div>
    </div>
    </div>
    </div>
    `).join('');
    
    document.getElementById('list-voucher').innerHTML = skeletonHTML;
    document.getElementById('list-pulsa').innerHTML = skeletonHTML;
    }
    
    // ============================================
    // RENDER GAME CARDS
    // ============================================
    function renderGameCards() {
    // Populer
    document.getElementById('list-populer').innerHTML = dbPopuler.map(g => `
    <div class="swiper-slide" data-badge="${g.b}">
    <div class="game-card">
    <div class="card-badge">${g.b}</div>
    <img src="${g.i}" loading="lazy" onload="this.classList.add('loaded')" onerror="this.src='https://via.placeholder.com/300x400?text=Image+Not+Found'; this.classList.add('loaded')">
    <div class="card-info">
    <h3>${g.n}</h3>
    <button class="btn-pilih" data-game="${g.n}">Pilih</button>
    </div>
    </div>
    </div>
    `).join('');
    
    // Terbaru
    document.getElementById('list-terbaru').innerHTML = dbTerbaru.map(g => `
    <div class="swiper-slide" data-badge="${g.b}">
    <div class="game-card">
    <div class="card-badge">${g.b}</div>
    <img src="${g.i}" loading="lazy" onload="this.classList.add('loaded')" onerror="this.src='https://via.placeholder.com/300x400?text=Image+Not+Found'; this.classList.add('loaded')">
    <div class="card-info">
    <h3>${g.n}</h3>
    <button class="btn-pilih" data-game="${g.n}">Pilih</button>
    </div>
    </div>
    </div>
    `).join('');
    
    // PC
    document.getElementById('list-pc').innerHTML = dbPC.map(g => `
    <div class="swiper-slide" data-badge="${g.b}">
    <div class="game-card">
    <div class="card-badge">${g.b}</div>
    <img src="${g.i}" loading="lazy" onload="this.classList.add('loaded')" onerror="this.src='https://via.placeholder.com/300x400?text=Image+Not+Found'; this.classList.add('loaded')">
    <div class="card-info">
    <h3>${g.n}</h3>
    <button class="btn-pilih" data-game="${g.n}">Pilih</button>
    </div>
    </div>
    </div>
    `).join('');
    }
    
    // ============================================
    // RENDER VOUCHER DAN PULSA
    // ============================================
    async function renderVoucherAndPulsa() {
    // Voucher
    document.getElementById('list-voucher').innerHTML = dbVoucher.map((v, index) => `
    <div class="swiper-slide">
    <div class="voucher-card skeleton" id="voucher-${index}">
    <img data-src="${v.i}" class="lazy-img">
    <div class="voucher-title">${v.n}</div>
    </div>
    </div>
    `).join('');
    
    // Pulsa
    document.getElementById('list-pulsa').innerHTML = dbPulsa.map((p, index) => `
    <div class="swiper-slide">
    <div class="voucher-card skeleton" id="pulsa-${index}">
    <img data-src="${p.i}" class="lazy-img pulsa-img">
    <div class="voucher-title">${p.n}</div>
    </div>
    </div>
    `).join('');
    
    // Load images with delay
    const lazyImages = document.querySelectorAll('.lazy-img');
    for (let i = 0; i < lazyImages.length; i++) {
    const img = lazyImages[i];
    const src = img.dataset.src;
    const card = img.closest('.voucher-card');
    try {
    await preloadImage(src);
    setTimeout(() => {
    img.src = src;
    img.classList.add('loaded');
    card.classList.remove('skeleton');
    }, i * 50);
    } catch (e) {
    img.src = 'https://via.placeholder.com/300x300?text=Error';
    img.classList.add('loaded');
    card.classList.remove('skeleton');
    }
    }
    
    swVoucher.update();
    swPulsa.update();
    swVoucher.updateSize();
    swPulsa.updateSize();
    }
    
    // ============================================
    // RENDER SEMUA KONTEN
    // ============================================
    async function renderAllContent() {
    renderGameCards();
    await renderVoucherAndPulsa();
    
    swPopuler.update();
    swTerbaru.update();
    swPC.update();
    }
    
    // ============================================
    // INIT SWIPER
    // ============================================
    const config = { 
    slidesPerView: 2.3, 
    spaceBetween: 12, 
    pagination: { el: ".swiper-pagination", clickable: true }, 
    breakpoints: { 
    640: { slidesPerView: 3 },
    768: { slidesPerView: 4 },
    1024: { slidesPerView: 5 } 
    } 
    };
    
    const swPopuler = new Swiper(".swiperPopuler", config);
    const swTerbaru = new Swiper(".swiperTerbaru", config);
    const swPC = new Swiper(".swiperPC", config);
    const swVoucher = new Swiper(".swiperVoucher", { ...config, slidesPerView: 2.5 });
    const swPulsa = new Swiper(".swiperPulsa", { ...config, slidesPerView: 2.5 });
    
    // Hero Swiper
    new Swiper(".hero-swiper", {
    loop: true,
    autoplay: { delay: 5000, disableOnInteraction: false },
    pagination: { el: ".hero-pagination", clickable: true },
    navigation: { nextEl: ".swiper-button-next", prevEl: ".swiper-button-prev" },
    effect: "fade",
    speed: 1000
    });
    
    // ============================================
    // START LOADING
    // ============================================
    renderSkeletons();
    setTimeout(renderAllContent, 1200);
    
    // ============================================
    // FILTER FUNCTION - UPDATE UNTUK SEMUA KATEGORI
    // ============================================
    function applyFilter(filterValue) {
    const isAll = filterValue === 'all';
    const isPulsa = filterValue === 'PULSA';
    const isPC = filterValue === 'PC';
    
    // Hitung jumlah visible per section
    let visiblePopuler = 0;
    let visibleTerbaru = 0;
    let visiblePC = 0;
    
    // Filter Populer
    document.querySelectorAll('.swiperPopuler .swiper-slide').forEach(slide => {
    const badge = slide.dataset.badge;
    if (isAll || badge === filterValue) {
    slide.classList.remove('hidden-slide');
    visiblePopuler++;
    } else {
    slide.classList.add('hidden-slide');
    }
    });
    
    // Filter Terbaru
    document.querySelectorAll('.swiperTerbaru .swiper-slide').forEach(slide => {
    const badge = slide.dataset.badge;
    if (isAll || badge === filterValue) {
    slide.classList.remove('hidden-slide');
    visibleTerbaru++;
    } else {
    slide.classList.add('hidden-slide');
    }
    });
    
    // Filter PC
    document.querySelectorAll('.swiperPC .swiper-slide').forEach(slide => {
    const badge = slide.dataset.badge;
    if (isAll || badge === filterValue) {
    slide.classList.remove('hidden-slide');
    visiblePC++;
    } else {
    slide.classList.add('hidden-slide');
    }
    });
    
    // Hide/show sections
    if (isPulsa) {
    // Hanya tampilkan section pulsa
    document.getElementById('sec-populer').classList.add('hidden-section');
    document.getElementById('sec-terbaru').classList.add('hidden-section');
    document.getElementById('sec-pc').classList.add('hidden-section');
    document.getElementById('sec-voucher').classList.add('hidden-section');
    document.getElementById('sec-pulsa').classList.remove('hidden-section');
    } else {
    // Tampilkan section yang memiliki konten
    document.getElementById('sec-populer').classList.toggle('hidden-section', visiblePopuler === 0 && !isAll);
    document.getElementById('sec-terbaru').classList.toggle('hidden-section', visibleTerbaru === 0 && !isAll);
    document.getElementById('sec-pc').classList.toggle('hidden-section', visiblePC === 0 && !isAll);
    document.getElementById('sec-voucher').classList.toggle('hidden-section', !isAll);
    document.getElementById('sec-pulsa').classList.add('hidden-section');
    }
    
    // Update swiper
    swPopuler.update();
    swTerbaru.update();
    swPC.update();
    swVoucher.update();
    swPulsa.update();
    
    // Reset ke slide pertama
    swPopuler.slideTo(0);
    swTerbaru.slideTo(0);
    swPC.slideTo(0);
    
    // Update URL hash (opsional)
    window.location.hash = filterValue;
    }
    
    // ============================================
    // CATEGORY BADGES CLICK EVENT
    // ============================================
    document.querySelectorAll('.cat-badge').forEach(btn => {
    btn.addEventListener('click', function() {
    document.querySelectorAll('.cat-badge').forEach(b => b.classList.remove('active'));
    this.classList.add('active');
    applyFilter(this.dataset.filter);
    });
    });
    
    // ============================================
    // BADGE CLICK EVENT (DI DALAM CARD)
    // ============================================
    document.addEventListener('click', function(e) {
    if (e.target.classList.contains('card-badge')) {
    const badgeText = e.target.textContent.trim();
    applyFilter(badgeText);
    
    // Aktifkan badge yang sesuai di category
    document.querySelectorAll('.cat-badge').forEach(cb => {
    cb.classList.remove('active');
    if (cb.dataset.filter === badgeText) {
    cb.classList.add('active');
    }
    });
    }
    });
    
    // ============================================
    // CHECK URL HASH SAAT LOAD
    // ============================================
    if (window.location.hash) {
    const hash = window.location.hash.substring(1); // remove #
    const targetBadge = document.querySelector(`.cat-badge[data-filter="${hash}"]`);
    if (targetBadge) {
    targetBadge.click();
    }
    }
    
    // ============================================
    // REDIRECT KE CHECKOUT SAAT KLIK TOMBOL PILIH
    // ============================================
    document.addEventListener('click', function(e) {
    if (e.target.classList.contains('btn-pilih')) {
    e.preventDefault();
    const gameName = e.target.dataset.game || e.target.closest('.game-card').querySelector('h3').textContent;
    
    // Redirect ke halaman checkout dengan parameter game
    const gameSlug = gameName.toLowerCase().replace(/ /g, '_').replace(/[^a-z0-9_]/g, '');
    window.location.href = '<?= base_url("payment/checkout/") ?>' + gameSlug;
    }
    });
    
    // ============================================
    // LIVE SEARCH FUNCTION
    // ============================================
    const searchInput = document.getElementById('searchInput');
    const searchBtn = document.querySelector('.search-btn');
    
    // Hapus container lama jika ada
    const oldContainer = document.querySelector('.search-results');
    if (oldContainer) oldContainer.remove();
    
    // Buat container hasil pencarian
    const searchResultsContainer = document.createElement('div');
    searchResultsContainer.className = 'search-results';
    document.querySelector('.search-container').appendChild(searchResultsContainer);
    
    // Database gambar untuk search
    const gameImages = {
    'mobile legends': 'assets/img/ml.jpg',
    'free fire': 'assets/img/freefire.png',
    'pubg mobile': 'assets/img/pubg.jpg',
    'valorant': 'assets/img/valo.jpg',
    'genshin impact': 'assets/img/genshin.jpeg',
    'call of duty': 'assets/img/callof.png',
    'apex legends': 'assets/img/apex.jpeg',
    'clash of clans': 'assets/img/coc.png',
    'honor of kings': 'assets/img/hok.png',
    'roblox': 'assets/img/roblox.jpg',
    'point blank': 'assets/img/pb.png',
    'dota 2': 'assets/img/dota.jpg',
    'counter-strike 2': 'assets/img/cs.jpg',
    'delta force': 'assets/img/delta.png',
    'arena of valor': 'assets/img/aov.png',
    'spotify': 'assets/img/spotify.jpg',
    'netflix': 'assets/img/netflix.jpg',
    'playstation': 'assets/img/playstation.jpg',
    'google play': 'assets/img/googleplay.png',
    'telkomsel': 'assets/img/telkomsel.png',
    'indosat': 'assets/img/indosat.png',
    'xl axiata': 'assets/img/xl.png',
    'tri': 'assets/img/tri.png',
    'smartfren': 'assets/img/smartfren.png'
    };
    
    // Fungsi mencari gambar
    function getGameImage(gameName) {
    const key = gameName.toLowerCase();
    if (gameImages[key]) return gameImages[key];
    
    for (let [k, v] of Object.entries(gameImages)) {
    if (key.includes(k) || k.includes(key)) {
    return v;
    }
    }
    return 'assets/img/game-default.jpg';
    }
    
    // Fungsi melakukan pencarian
    function performSearch(query) {
    if (query.length < 2) {
    searchResultsContainer.style.display = 'none';
    return;
    }
    
    // Tampilkan loading
    searchResultsContainer.innerHTML = '<div class="search-loading"><div class="loading-spinner"></div><span>Mencari...</span></div>';
    searchResultsContainer.style.display = 'block';
    
    // Gabungkan semua database untuk pencarian lokal
    const allItems = [
    ...dbPopuler.map(g => ({ name: g.n, category: 'Game Populer', image: g.i })),
    ...dbTerbaru.map(g => ({ name: g.n, category: 'Game Terbaru', image: g.i })),
    ...dbPC.map(g => ({ name: g.n, category: 'Game PC', image: g.i })),
    ...dbVoucher.map(v => ({ name: v.n, category: 'Voucher Digital', image: v.i })),
    ...dbPulsa.map(p => ({ name: p.n, category: 'Pulsa & Data', image: p.i }))
    ];
    
    // Filter berdasarkan query
    const results = allItems.filter(item => 
    item.name.toLowerCase().includes(query.toLowerCase())
    );
    
    setTimeout(() => {
    if (results.length > 0) {
    let html = '<div class="search-results-header">Hasil Pencarian</div>';
    
    results.slice(0, 8).forEach(item => {
    const gameImage = getGameImage(item.name) || item.image;
    html += `
    <a href="#" class="search-result-item" data-game="${item.name}">
    <div class="result-image">
    <img src="${gameImage}" alt="${item.name}" onerror="this.src='https://via.placeholder.com/50x50?text=Game'">
    </div>
    <div class="result-info">
    <div class="result-name">${item.name}</div>
    <div class="result-category">${item.category}</div>
    </div>
    <div class="result-action">
    <span>Pilih</span>
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor">
    <path d="M5 12h14M12 5l7 7-7 7" stroke-width="2"/>
    </svg>
    </div>
    </a>
    `;
    });
    
    searchResultsContainer.innerHTML = html;
    
    // Tambahkan event listener untuk hasil pencarian
    document.querySelectorAll('.search-result-item').forEach(item => {
    item.addEventListener('click', function(e) {
    e.preventDefault();
    const gameName = this.dataset.game;
    const gameSlug = gameName.toLowerCase().replace(/ /g, '_').replace(/[^a-z0-9_]/g, '');
    window.location.href = '<?= base_url("payment/checkout/") ?>' + gameSlug;
    });
    });
    } else {
    searchResultsContainer.innerHTML = `
    <div class="no-results">
    <svg width="50" height="50" viewBox="0 0 24 24" fill="none" stroke="currentColor">
    <circle cx="12" cy="12" r="10"/>
    <line x1="12" y1="8" x2="12" y2="12"/>
    <circle cx="12" cy="16" r="1"/>
    </svg>
    <p>Game "${query}" tidak ditemukan</p>
    <small>Coba kata kunci lain</small>
    </div>
    `;
    }
    }, 300); // Simulasi loading
    }
    
    // Event listeners untuk search
    searchInput.addEventListener('input', function(e) {
    clearTimeout(window.searchTimeout);
    const query = this.value.trim();
    
    window.searchTimeout = setTimeout(() => {
    performSearch(query);
    }, 300);
    });
    
    searchBtn.addEventListener('click', function() {
    const query = searchInput.value.trim();
    if (query.length >= 2) {
    performSearch(query);
    }
    });
    
    // Klik di luar search results
    document.addEventListener('click', function(e) {
    if (!e.target.closest('.search-container')) {
    searchResultsContainer.style.display = 'none';
    }
    });
    
    searchInput.addEventListener('focus', function() {
    const query = this.value.trim();
    if (query.length >= 2) {
    performSearch(query);
    }
    });
    
    document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
    searchResultsContainer.style.display = 'none';
    }
    });
    
    // ============================================
    // DROPDOWN TOGGLE (User Menu)
    // ============================================
    const userBtn = document.getElementById('userBtn');
    const dropdownContent = document.getElementById('dropdownContent');
    const dropdownIcon = document.querySelector('.dropdown-icon');
    
    if (userBtn && dropdownContent) {
    userBtn.addEventListener('click', function(e) {
    e.stopPropagation();
    dropdownContent.classList.toggle('show');
    if (dropdownIcon) dropdownIcon.classList.toggle('active');
    });
    
    // Tutup dropdown kalau klik di luar
    document.addEventListener('click', function(e) {
    if (!userBtn.contains(e.target)) {
    dropdownContent.classList.remove('show');
    if (dropdownIcon) dropdownIcon.classList.remove('active');
    }
    });
    }
    
    // ============================================
    // UPDATE PROMO COUNT VIA AJAX (Opsional)
    // ============================================
    function updatePromoCount() {
    fetch('<?= base_url("promo/get_count") ?>')
    .then(response => response.json())
    .then(data => {
    const promoBadge = document.getElementById('promo-count');
    if (promoBadge) {
    if (data.count > 0) {
    promoBadge.textContent = data.count;
    promoBadge.style.display = 'inline-block';
    } else {
    promoBadge.style.display = 'none';
    }
    }
    })
    .catch(error => console.error('Error:', error));
    }
    
    // Update setiap 5 menit (opsional)
    // setInterval(updatePromoCount, 300000);
    
    // ============================================
    // SCROLL TO SECTION (Opsional)
    // ============================================
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
    e.preventDefault();
    const target = document.querySelector(this.getAttribute('href'));
    if (target) {
    target.scrollIntoView({
    behavior: 'smooth',
    block: 'start'
    });
    }
    });
    });
    
    // ============================================
    // HANDLE RESPONSIVE
    // ============================================
    function handleResize() {
    // Update swiper pada resize
    swPopuler.update();
    swTerbaru.update();
    swPC.update();
    swVoucher.update();
    swPulsa.update();
    }
    
    window.addEventListener('resize', handleResize);
    </script>


<script>
    // Munculkan pop-up saat halaman selesai dimuat
    window.onload = function() {
        Swal.fire({
            title: 'VORTEX PROMO!',
            text: 'Top up Diamond ML diskon 20% hari ini saja!',
            imageUrl: '<?= base_url("assets/img/promo/promo.png") ?>', // Ganti dengan path gambar promomu
            imageWidth: 400,
            imageHeight: 200,
            imageAlt: 'Promo Image',
            background: '#06080c', // Warna dark-bg kamu
            color: '#fff',
            confirmButtonText: 'GAS TOP UP!',
            confirmButtonColor: '#00f2fe',
            backdrop: `rgba(0,242,254,0.1) blur(4px)` // Efek blur futuristik
        });
    };
</script>

<script>
    // Tampilkan modal 2 detik setelah halaman dimuat
    setTimeout(() => {
        document.getElementById('vortexModal').style.display = 'flex';
    }, 2000);

    function closeModal() {
        document.getElementById('vortexModal').style.display = 'none';
    }

    // Tutup jika klik di luar area modal
    window.onclick = function(event) {
        if (event.target == document.getElementById('vortexModal')) {
            closeModal();
        }
    }
</script>
<script>
function adjustPlaceholder() {
    const searchInput = document.querySelector('.search-input');
    if (window.innerWidth >= 1024) {
        searchInput.placeholder = "Cari game atau voucher di sini...";
    } else {
        searchInput.placeholder = "Cari game";
    }
}

// Jalankan saat halaman dimuat
adjustPlaceholder();

// Jalankan saat layar di-resize (di BLFS/Firefox mungkin sering resize window)
window.addEventListener('resize', adjustPlaceholder);
</script>


<script>
// ============================================
// REDIRECT KE CHECKOUT DENGAN LOADING SPINNER SAJA
// ============================================
document.addEventListener('click', function(e) {
    if (!e.target.classList.contains('btn-pilih')) return;

    e.preventDefault();
    
    const btn = e.target;
    const originalText = btn.innerHTML;
    btn.disabled = true;
    btn.innerHTML = 'Memproses...';
    
    // Ambil nama game & buat slug (tetap pakai versi aman)
    let gameName = btn.dataset.game || 
                   btn.closest('.game-card')?.querySelector('h3')?.textContent || 
                   'game';

    const gameSlug = gameName
        .toLowerCase()
        .trim()
        .normalize("NFD")
        .replace(/[\u0300-\u036f]/g, "")
        .replace(/[^a-z0-9\s-]/g, '')
        .replace(/\s+/g, '-')
        .replace(/-+/g, '-')
        .replace(/^-+|-+$/g, '');

    // SweetAlert hanya spinner (tanpa title & text)
    Swal.fire({
        allowOutsideClick: false,
        allowEscapeKey: false,
        showConfirmButton: false,
        background: 'transparent',           // hilangkan background popup
        backdrop: `rgba(0, 242, 254, 0.12) blur(4px)`,  // tetap pakai efek blur neon
        didOpen: () => {
            Swal.showLoading();  // hanya spinner yang muncul
            
            setTimeout(() => {
                const baseUrl = window.APP?.base_url || '<?= base_url() ?>';
                window.location.href = baseUrl + 'payment/checkout/' + gameSlug;
            }, 9000);
        },
        willClose: () => {
            btn.disabled = false;
            btn.innerHTML = originalText;
        }
    });
});
</script>

<script>
// ============================================
// DROPDOWN CONTACT DI FOOTER - FIXED VERSION
// ============================================
document.addEventListener('DOMContentLoaded', function() {
    const contactToggle = document.getElementById('contactToggle');
    const contactDropdown = document.getElementById('contactDropdown');
    const contactIcon = document.querySelector('.dropdown-icon-contact');
    
    if (contactToggle && contactDropdown) {
        // Toggle dropdown saat tombol diklik
        contactToggle.addEventListener('click', function(e) {
            e.preventDefault();
            e.stopPropagation();
            
            contactDropdown.classList.toggle('show');
            
            if (contactIcon) {
                contactIcon.classList.toggle('active');
            }
        });
        
        // Tutup dropdown saat klik di luar
        document.addEventListener('click', function(e) {
            if (!contactToggle.contains(e.target) && !contactDropdown.contains(e.target)) {
                contactDropdown.classList.remove('show');
                if (contactIcon) {
                    contactIcon.classList.remove('active');
                }
            }
        });
        
        // Jangan tutup dropdown saat klik di dalam dropdown
        contactDropdown.addEventListener('click', function(e) {
            e.stopPropagation();
        });
    }
    
    // Link WhatsApp & Email tetap berfungsi
    const contactLinks = document.querySelectorAll('.contact-item');
    contactLinks.forEach(link => {
        link.addEventListener('click', function() {
            // Tutup dropdown setelah klik link
            setTimeout(() => {
                if (contactDropdown) {
                    contactDropdown.classList.remove('show');
                    if (contactIcon) {
                        contactIcon.classList.remove('active');
                    }
                }
            }, 100);
        });
    });
});
</script>



</body>
</html>